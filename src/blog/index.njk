---
layout: base.njk
title: Blog
description: Thoughts on faith, motherhood, and showing up imperfectly — by Irene Grace Daniels.
---

<section class="page-hero">
  <div class="container">
    <h1>Blog</h1>
    <p>Faith without the filter. Life without the pretense.</p>
  </div>
</section>

<section class="section">
  <div class="container container-narrow">
    
    {% if collections.posts.length > 0 %}
      <ul class="posts-list">
        {% for post in collections.posts %}
          <li class="post-card">
            <h2><a href="{{ post.url | url }}">{{ post.data.title }}</a></h2>
            <p class="post-meta">{{ post.date | dateFormat }}</p>
            {% if post.data.excerpt %}
              <p class="post-excerpt">{{ post.data.excerpt }}</p>
            {% endif %}
            <a href="{{ post.url | url }}" class="btn btn-secondary">Read More</a>
          </li>
        {% endfor %}
      </ul>
    {% else %}
      <div style="text-align: center; padding: var(--space-xl) 0;">
        <h2>Coming Soon</h2>
        <p>I'm working on some new content. In the meantime, check out my other blogs:</p>
        <div class="blogs-grid" style="margin-top: var(--space-md);">
          <a href="https://the-hot-mess.untitledpublishers.com" target="_blank" rel="noopener" class="blog-card">
            <h3>The Hot Mess</h3>
            <p>Faith, coffee, and the beautiful chaos of running a small business</p>
          </a>
          <a href="https://systems-under-siege.untitledpublishers.com" target="_blank" rel="noopener" class="blog-card">
            <h3>The Clean Enough House</h3>
            <p>Real talk for overwhelmed parents trying to manage a household</p>
          </a>
        </div>
      </div>
    {% endif %}
    
  </div>
</section>

<!-- Newsletter CTA -->
<section class="newsletter-section">
  <div class="container">
    <h2>Don't Miss a Post</h2>
    <p>Get new blog posts and updates delivered straight to your inbox.</p>
    <form class="newsletter-form" action="https://connect.untitledpublishers.com/subscribe" method="POST">
      <input type="hidden" name="list" value="irene-daniels">
      <input type="email" name="email" placeholder="Your email address" required>
      <button type="submit" class="btn btn-accent">Subscribe</button>
    </form>
  </div>
</section>
